@inherits LayoutComponentBase
@using RestaurantOrderManager.Client.Components.BaseComponents

<div class="page">
    @if (!IsLoginPage)
    {
        <div class="custom-sidebar">
            <CustomNav />
        </div>
    }

    <main class="@(IsLoginPage ? "main-content no-sidebar" : "main-content")">
        <article class="content px-4">
            @Body
        </article>
    </main>
</div>