@page "/order/{TableId:int}"
@rendermode InteractiveServer

<div class="row">
    @foreach (var menuItem in Menu)
    {
        <div class="col-lg-2 col-md-6 col-sm-12 mb-4">
            <div class="card d-flex flex-column h-100 shadow-sm bg-light">
                <div class="card-body d-flex flex-column text-center">
                    <h6 class="card-title">@menuItem.Name</h6>
                    <p class="card-text">@menuItem.Price €</p>
                    <button class="btn btn-outline-primary mt-auto" @onclick="() => AddToCart(menuItem)">Add</button>
                </div>
            </div>
        </div>
    }
</div>
<ul>
    @foreach (var item in CartItems)
    {
        <li>
            @item.Quantity x @item.Name (@item.Price €) = @item.Total €
            <button class="btn btn-secondary btn-sm" @onclick="() => DecreaseItemAmount(item.MenuItemId)">-</button>
            <button class="btn btn-secondary btn-sm" @onclick="() => IncreaseItemAmount(item.MenuItemId)">+</button>
            <button class="btn btn-danger btn-sm" @onclick="() => RemoveFromCart(item.MenuItemId)">x</button>
        </li>
    }
</ul>
<p>Total: @CartTotal €</p>
<button @onclick="ConfirmOrder" disabled="@(CartItems == null || CartItems.Count == 0)">Confirm Order</button>

